== Congress

This application loads bills and legislators from the OpenSunlight Congress API.
Users are able to then log in and tag bills or legislators they wish to receive
updates on.

To set up this application be sure to run the rake tasks to set up the
database.

